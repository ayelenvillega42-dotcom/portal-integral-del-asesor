export const metadata = {
  title: "Portal Integral del Asesor",
  description: "Portal Integral del Asesor",
};

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}
